<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- inside color -->
    <stroke
        android:width="0.5dp"
        android:color="@color/borderColor" /> <!-- border color -->
    <corners android:radius="10dp"/> <!-- same as cardCornerRadius -->
</shape>
