<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Light Theme -->
    <style name="Theme.VSL" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Month title (header) -->
    <style name="CalendarHeaderText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/inter_24pt_medium</item>
    </style>

    <!-- Weekdays (Mon-Sun) -->
    <style name="CalendarWeekDayText">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/inter_24pt_regular</item>
        <!-- Default color will be overridden for weekends via decorator -->
        <item name="android:textColor">@color/mainColor</item>
    </style>

    <!-- Dates -->
    <style name="CalendarDateText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">14sp</item>
    </style>

</resources>
